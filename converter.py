import model
import utils

class Converter():
    def __init__(self, model_dir):
        '''
            Initialize and load model
            model are stored in 'model_dir'
        '''
        pass
    def convert(f0, timeaxis, sps, ap):
        '''
            Give f0, timeaxis, sps, ap
            Reture converted f0, timeaxis, sps, ap
        '''
        return c_f0, c_timeaxis, c_sps, c_ap